*.iml
.gradle
/local.properties
/.idea
.DS_Store
/build
/buildSrc/build
/captures
.externalNativeBuild
.cxx
*.xcuserstate
*.xcbkptlist
!/.idea/codeStyles/*
!/.idea/inspectionProfiles/*
.kotlin
.mimocode
.openclaude
/node_modules/
**/build/
/docs/

# Kotlin SwiftPM integration lock files — regenerated on every Gradle build.
.swiftpm-locks/

# Python virtual environments and bytecode caches
.venv/
__pycache__/
*.pyc

# Large ML model binaries. These are regenerated by the scripts under ml/ and
# plans/, and the original sources are linked from the *.md files next to them;
# the scripts, .md source links and small metadata (.json/.jpg) stay tracked.
*.onnx
*.pth
*.pt
*.tflite
*.pb
*.bin
*.safetensors
*.ckpt
*.h5
*.mlmodel
*.blob
*.tar.gz
variables.*

# Images used for model testing / benchmarks (test datasets + verification
# renders). Regenerated by the scripts under ml/ and plans/.
plans/samples/
plans/rtmpose_hand/output*/
ml/original_models/**/output_*.jpg
onnx/samples/

# Keep the small MediaPipe detector fixture used by instrumented tests.
!app/src/androidTest/assets/hand_landmarks_detector.tflite

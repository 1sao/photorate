package isao.photorate.galleryComponent.populateGallery

/** Per-volume MediaStore generation numbers captured before the corresponding queries. */
actual class Checkpoint(val generations: Map<String, Long>)
